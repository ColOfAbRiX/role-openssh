---

openssh_packages:
 - "openssh-server{% if ssh_version not in ['', 'latest'] %}-{{ ssh_version }}{% endif %}"
 - "openssh-clients{% if ssh_version not in ['', 'latest'] %}-{{ ssh_version }}{% endif %}"

openssh_sshd_service: sshd
